# Cloudflare/Wrangler local auth + state
.wrangler/

# Local env/secrets
.env
.env.*
!.env.example
.dev.vars
.secrets*

# Oden demo HTML is private: bundled into the oden-demo worker at deploy time,
# never published via GitHub Pages (the whole repo root is served publicly).
workers/oden/demo.html
apps/OdenProStudios/

# Private key material
*.pem
*.key
*.p12
*.pfx
id_rsa
id_ed25519
